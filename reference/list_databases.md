# List supported gene-set databases

Returns a tibble with metadata for every registered database.

## Usage

``` r
list_databases()
```

## Value

Tibble with columns: name, display_name, type, species, source, license,
citation_key, description_short.
