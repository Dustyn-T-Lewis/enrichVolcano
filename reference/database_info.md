# Print extended description for one database

Print extended description for one database

## Usage

``` r
database_info(name)
```

## Arguments

- name:

  Database name (see `list_databases()$name`).

## Value

Invisibly, the registry row for `name`.
