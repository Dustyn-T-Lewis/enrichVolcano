% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_labels.R
\name{list_labels}
\alias{list_labels}
\alias{write_labels}
\title{List the term labels a figure will draw, to review or edit}
\usage{
list_labels(
  enrichment,
  contrast = NULL,
  databases = NULL,
  collapse = TRUE,
  term_threshold = 0.05,
  n_terms = 12
)

write_labels(enrichment, file, ...)
}
\arguments{
\item{enrichment}{An enrichment object from \code{\link[=as_enrichment]{as_enrichment()}} or \code{\link[=run_enrichment]{run_enrichment()}}.}

\item{contrast}{Contrasts to list; \code{NULL} (default) lists every contrast.}

\item{databases}{Collections to draw from, matched against the \code{database}
column, e.g. \code{c("Hallmark", "GO Slim")}. \code{NULL} (default) draws from all.
Skipped, with a note, when the object has no database labels.}

\item{collapse}{Hide terms flagged \code{"redundant"} by \code{\link[=dedup_terms]{dedup_terms()}}.}

\item{term_threshold}{Terms need \code{padj} below this to be drawn.}

\item{n_terms}{Most unique terms per contrast, as on the ring. \code{Inf} lists
every significant term.}

\item{file}{Path of the CSV to write.}

\item{...}{Arguments passed on to \code{list_labels()}.}
}
\value{
A data frame with one row per term: \code{database}, \code{term},
\code{contrasts} (the contrasts that draw it, joined by \verb{;}), \code{clean}, \code{short}
and \code{label}, a copy of \code{short} to edit.

\code{write_labels()}: \code{file}, invisibly.
}
\description{
Picks terms the way \code{\link[=plot_volcano_ring]{plot_volcano_ring()}} does, for each contrast, and lists
each term once with both shipped styles from \code{\link[=clean_label]{clean_label()}}. Edit the
\code{label} column, in R or in the CSV that \code{write_labels()} writes, and pass
the table back through \code{labels} of \code{\link[=plot_volcano_ring]{plot_volcano_ring()}} or
\code{\link[=plot_scatter]{plot_scatter()}}. A line break is written as the two characters \verb{\\n}.
}
\examples{
ex <- as_enrichment(read.csv(system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)))
head(list_labels(ex))
}
