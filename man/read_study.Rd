% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_study.R
\name{read_study}
\alias{read_study}
\title{Read a study from a workbook or a folder of CSV files}
\usage{
read_study(path)
}
\arguments{
\item{path}{An \code{.xlsx} workbook with sheets named as above, or a folder of
\verb{<sheet>.csv} or \verb{<sheet>.csv.gz} files.}
}
\value{
A list of class \code{enrichVolcano_study} with \code{da}, \code{matrix},
\code{samples}, \code{contrasts} and \code{weights} (absent parts are \code{NULL}).
}
\description{
A study is up to five tables, linked by name:
\itemize{
\item \code{da_results} (required): differential-abundance results in any format
\code{\link[=as_da]{as_da()}} reads, with a contrast column.
\item \code{matrix}: one row per protein; the first column holds the protein IDs and
every other column is a sample.
\item \code{samples}: \code{sample} and \code{group}, optionally \code{subject} for repeated
measures, plus any covariate columns.
\item \code{contrasts}: \code{name} and \code{expression}, arithmetic on group names such as
\code{Post - Pre} or \code{(B_Post - B_Pre) - (A_Post - A_Pre)}.
\item \code{weights} (optional): precision weights shaped like \code{matrix}.
}
}
\details{
\code{da_results} alone is enough for fgsea and the figures; fry and camera also
need \code{matrix}, \code{samples} and \code{contrasts}. Every link is checked by name:
matrix columns against \code{samples$sample}, contrast expressions against
\code{samples$group}, \code{da_results} contrasts against \code{contrasts$name}, and
\code{weights} against \code{matrix}. Group names must be syntactic (letters, digits,
\code{.} and \verb{_}), because \code{-} inside a name would read as subtraction.
}
