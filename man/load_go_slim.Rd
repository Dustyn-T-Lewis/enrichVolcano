% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/go_slim.R
\name{load_go_slim}
\alias{load_go_slim}
\title{Load canonical generic GO slim gene sets}
\usage{
load_go_slim(
  species = c("human", "mouse", "rat"),
  min_size = 10L,
  max_size = 500L
)
}
\arguments{
\item{species}{One of \code{"human"}, \code{"mouse"}, or \code{"rat"}.}

\item{min_size}{Minimum gene-set size (default \code{10}).}

\item{max_size}{Maximum gene-set size (default \code{500}). Broad parents
(\verb{>500} genes) are too non-specific to be informative in enrichment.}
}
\value{
Named list of character vectors; names are prefixed \code{GOSLIM_}.
}
\description{
Parses the shipped \code{inst/extdata/goslim_generic.obo} (the official GO
Consortium generic slim, frozen for reproducibility — see
\code{inst/extdata/goslim_generic.PROVENANCE.md}) to obtain biological_process
slim term IDs, expands each term to include all its BP offspring via
\code{GO.db::GOBPOFFSPRING}, then maps genes to symbols via the species
\code{org.db}. Term IDs come exclusively from the OBO file — no inline list.
}
\details{
This replaces the previous build-time \code{data-raw/build_go_slim.R} which
shipped pre-built GMTs from a hardcoded 62-term ID vector. The runtime
loader is fully canonical: the GO Consortium publishes the generic slim,
and \code{GO.db} / \code{org.db} are versioned Bioconductor annotation packages.
}
