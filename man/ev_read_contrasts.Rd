% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_contrasts.R
\name{ev_read_contrasts}
\alias{ev_read_contrasts}
\title{Read per-contrast CSVs into a single validated long table}
\usage{
ev_read_contrasts(
  path,
  species,
  p_adjust = "BH",
  uniprot = NULL,
  x = NULL,
  y = NULL,
  padj = NULL,
  pattern = "\\\\.csv$"
)
}
\arguments{
\item{path}{A directory (all matching files are read) or a character vector
of file paths.}

\item{species}{One of \code{"human"}, \code{"mouse"}, \code{"rat"}.}

\item{p_adjust}{Adjustment method passed to \code{\link[=adjust_p]{adjust_p()}} when no adjusted
column is present (default \code{"BH"}).}

\item{uniprot, x, y}{Optional column-name overrides for the accession, logFC,
and p-value columns (passed to \code{\link[=ev_validate]{ev_validate()}}).}

\item{padj}{Optional name of an existing adjusted-p column to use as
\code{adj.P.Val}.}

\item{pattern}{File-matching regex when \code{path} is a directory.}
}
\value{
A validated long tibble with \code{uniprot}, \code{symbol}, \code{gene}, \code{contrast},
\code{logFC}, \code{P.Value}, \code{adj.P.Val}, and an \code{ev_idmap_report} attribute.
}
\description{
Each CSV is one contrast; the file name (without extension) becomes the
contrast label. Required columns: a UniProt accession column, \code{logFC}, and a
p-value. An adjusted p-value column is optional; when absent it is computed
with \code{\link[=adjust_p]{adjust_p()}}. Accessions are mapped to gene symbols via the bundled
per-species table (see \code{\link[=ev_idmap_report]{ev_idmap_report()}} for the mapping summary).
}
