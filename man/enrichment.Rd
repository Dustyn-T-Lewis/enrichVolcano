% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{enrichment}
\alias{enrichment}
\title{Enrichment results in one validated object}
\usage{
enrichment(results = data.frame(), metadata = list())
}
\arguments{
\item{results}{A data frame with one row per term per contrast:
\itemize{
\item \code{contrast}, \code{database}, \code{term}: character. \code{database} may be \code{NA}.
\item \code{score}: numeric. NES for fgsea and clusterProfiler; signed
\eqn{-\log_{10}}(FDR) for limma rotation/competitive tests and ORA.
\item \code{p}, \code{padj}: numeric in [0, 1]; \code{NA} allowed.
\item \code{size}: numeric set size.
\item \code{direction}: \code{"up"} or \code{"down"}, agreeing with the sign of \code{score}.
\item \code{leading_edge}: list of character vectors, empty when the test has none.
}

Extra columns (for example \code{dedup_status} or your own groupings) are kept
and can be mapped by the plots.}

\item{metadata}{A list with \code{enrichment_test} (the method that produced the
results), \code{score_type} (the axis and legend label for \code{score}), and
\code{dedup} (\code{NULL}, or the settings \code{dedup()} used).}
}
\value{
An S7 object with properties \code{results} and \code{metadata}. Every
construction and every edit is validated.
}
\description{
\code{enrichment} is the single input every plot in the package reads. Build it
with \code{\link[=as_enrichment]{as_enrichment()}} rather than by hand; construct it directly only when
you already hold a table in exactly this shape.
}
