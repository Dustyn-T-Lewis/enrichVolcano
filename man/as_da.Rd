% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_da.R
\name{as_da}
\alias{as_da}
\title{Standardise differential-abundance results}
\usage{
as_da(
  x,
  contrast = NULL,
  species = "Homo sapiens",
  protein = NULL,
  gene = NULL,
  logfc = NULL,
  t = NULL,
  p = NULL,
  padj = NULL,
  abundance = NULL
)
}
\arguments{
\item{x}{A results table with a contrast column (\code{contrast} or MSstats'
\code{Label}), a single-contrast table with \code{contrast} set, or a named list of
tables whose names are the contrasts.}

\item{contrast}{Label for a table that has no contrast column.}

\item{species}{For UniProt accessions, the species whose annotation
package supplies current gene symbols (\code{"Homo sapiens"}, \code{"Mus musculus"},
\code{"Rattus norvegicus"}). \code{NULL} keeps the table's own symbols. Tables keyed
by symbols rather than accessions keep theirs either way.}

\item{protein, gene, logfc, t, p, padj, abundance}{Column names, when detection
does not find them. \code{logfc} names the fold-change column.}
}
\value{
A data frame of class \code{enrichVolcano_da} with columns \code{protein},
\code{gene}, \code{contrast}, \code{logFC}, \code{t}, \code{p}, \code{padj}, \code{abundance}, \code{rank} and
\code{rank_stat}, followed by any input columns it did not use.
}
\description{
Reads a DA results table from any common proteomics tool and returns it with
one set of column names, so the plots and \code{\link[=run_enrichment]{run_enrichment()}} can read it.
}
\section{Recognised tools}{

limma and limpa \code{topTable()} (IDs in the row names, or an unnamed first
column once written to CSV), proteoDA results and per-contrast CSVs, MSstats
\code{groupComparison()}, proDA \code{test_diff()}, msqrob2 \code{topFeatures()}, prolfQua
contrast tables and ProtRank output. Any other table works through the
column arguments. F-test tables (no fold change) and wide tables with one
column per contrast are refused; reshape them to one row per protein and
contrast first.
}

\section{Ranking statistic}{

\code{rank} is the moderated t when the table has one, else
\code{sign(logFC) * -log10(p)}, else \code{sign(logFC) * -log10(padj)}; \code{rank_stat}
says which. The signed p-value ranking is the one Reimand et al. (2019)
recommend for GSEA.
}

\section{Gene symbols}{

Gene sets list gene symbols, so each UniProt accession is mapped to its
current symbol through the species' annotation package (isoform suffixes
such as \code{-2} are dropped first). A symbol from the search engine's FASTA can
be out of date: \code{O00483} is \code{COXFA4}, formerly \code{NDUFA4}.
}

\examples{
tbl <- data.frame(
  logFC = c(1.2, -0.8), t = c(4.1, -3.2), P.Value = c(1e-4, 2e-3),
  adj.P.Val = c(1e-3, 0.01), row.names = c("P31040", "Q9UBK2")
)
as_da(tbl, contrast = "Aging")
}
\references{
Reimand J, Isserlin R, Voisin V, et al. (2019). Pathway enrichment analysis
and visualization of omics data using g:Profiler, GSEA, Cytoscape and
EnrichmentMap. Nature Protocols 14:482-517. \doi{10.1038/s41596-018-0103-9}
}
