% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrich_volcano.R
\name{enrich_volcano}
\alias{enrich_volcano}
\title{Build a composite volcano + enrichment ring plot}
\usage{
enrich_volcano(
  data,
  contrast,
  species = "human",
  databases = c("hallmark", "reactome", "go_bp"),
  x = NULL,
  y = NULL,
  lab = NULL,
  uniprot = NULL,
  p_method = c("pi_eq2", "pi_eq1", "raw_p", "adj_p"),
  p_adjust = "BH",
  p_threshold = 0.05,
  logfc_threshold = log2(1.5),
  enrich_mode = c("fgsea", "ora"),
  enrich_padj = 0.05,
  rank_by = "t",
  dedup = list(cutoff = 0.5, scope = "within_db"),
  ring = list(max_terms = 10),
  facet = list(nrow = NULL, ncol = NULL),
  disc_color = NULL,
  nes_limits = c(-3, 3),
  subtitle = NULL,
  tag = NULL,
  count_x_mult = 0.5,
  count_y_mult = 1,
  label_mode = c("none", "top_per_direction", "top_total", "all_significant", "explicit"),
  label_n = 10,
  label_rank_by = c("significance", "logfc"),
  label_genes = NULL,
  label_size = 2.6,
  point_size = 0.9,
  point_alpha = 0.6,
  theme = ev_theme()
)
}
\arguments{
\item{data}{Differential abundance results. Accepts tidy long, wide-suffix,
DEP, proteoDA, limma, DESeq2, edgeR, MSstats, proDA, DEqMS, MaxQuant,
or Perseus formats - \code{ev_validate()} auto-detects.}

\item{contrast}{Character; one or many contrast names.}

\item{species}{Character; "human" (default), "mouse", "rat", "zebrafish",
"fly", "yeast", "pig". UniProt accession mapping is available for \code{human},
\code{mouse}, and \code{rat}; other species are supported for enrichment but require
gene-symbol input.}

\item{databases}{Character vector of registered DB names (see
\code{list_databases()}), or a named list of pathway lists.}

\item{x, y, lab}{Column names for logFC, p-value, gene label (auto-detected
if NULL).}

\item{uniprot}{Column name holding UniProt accessions (auto-detected by default).}

\item{p_method}{Y-axis transform: \code{"pi_eq2"} (default), \code{"pi_eq1"},
\code{"raw_p"}, \code{"adj_p"}.}

\item{p_adjust}{Method: \code{"BH"} (default), \code{"bonferroni"}, \code{"qvalue"}, \code{"IHW"}.}

\item{p_threshold, logfc_threshold}{Volcano significance cutoffs. A point is
coloured up/down only when it clears both \code{p_threshold} and
\code{abs(logFC) >= logfc_threshold}.}

\item{enrich_mode}{\code{c("fgsea", "ora")} - either or both.}

\item{enrich_padj}{Pathway significance cutoff (default 0.05).}

\item{rank_by}{Signed statistic fgsea ranks genes by. \code{"t"} (default, the
limma / proteoDA moderated t-statistic; falls back to \code{"signed_p"} when no
\code{t} column is present), \code{"signed_p"} (\code{sign(logFC) * -log10(P)}), \code{"logFC"},
or any signed column. Unsigned pi-score columns are rejected.}

\item{dedup}{List forwarded to \code{\link[=ev_collapse]{ev_collapse()}}; recognised fields are
\code{method}, \code{cutoff}, \code{scope}, \code{sig_threshold}, \code{collapse_pval_threshold},
\code{keep_by}, \code{similarity}, and \code{combined_weight}. \code{method} may be
\code{"collapse_then_jaccard"} (default), \code{"jaccard_then_collapse"},
\code{"jaccard"}, or \code{"collapse_fgsea"}; \code{"both"} is a deprecated alias for
\code{"jaccard_then_collapse"}. \code{similarity} selects the gene-overlap metric
(\code{"jaccard"}, \code{"overlap"}, \code{"combined"}; Merico 2010). When a field is
omitted, the \code{\link[=ev_collapse]{ev_collapse()}} default is used. See \code{\link[=ev_collapse]{ev_collapse()}} for the
full semantics.}

\item{ring}{List controlling the ring. Honoured fields: \code{max_terms} (cap on
pathways drawn in the ring); \code{show_databases} (default NULL = all enriched
databases; pass a character vector to restrict the ring to those named
databases); \code{direction_balance} (default FALSE = global top-N by padj;
TRUE = balanced selection of top up-NES + top down-NES terms; rows without
a signed NES, such as ORA results, are excluded from balanced selection).
Arc height
is fixed to \code{-log10(padj)} and fill to NES by the canonical layout. For
tunable \code{order_by}/\code{magnitude}/\code{color} encodings use the standalone
\code{\link[=ring_plot]{ring_plot()}}.}

\item{facet}{List with \code{nrow}, \code{ncol} for patchwork outer layout.}

\item{disc_color}{Optional contrast-tint for the ring's central disc.}

\item{nes_limits}{Length-2 NES colour-scale limits (default \code{c(-3, 3)});
widen when enrichment exceeds |NES| = 3 to avoid saturating the scale.}

\item{subtitle}{Optional subtitle. When \code{contrast} is a single name, the
string is rendered verbatim; for multi-contrast input, pass a named
character vector or NULL.}

\item{tag}{Optional panel tag (e.g. \code{"A"}); single-character for a single
contrast, named character vector for multi-contrast.}

\item{count_x_mult, count_y_mult}{Position of the up/down count boxes,
forwarded to \code{\link[=ev_volcano_ring]{ev_volcano_ring()}}. Defaults \verb{(0.5, 1.0)}; the YvO 2026
panel layout uses \verb{(0.85, 0.75)}.}

\item{label_mode, label_n, label_rank_by, label_genes}{Volcano point labeling,
passed to the shared selector. \code{label_mode = "none"} (default) draws no
protein labels; \code{"top_per_direction"} / \code{"top_total"} label the top
\code{label_n} by \code{label_rank_by}; \code{"all_significant"} labels every hit;
\code{"explicit"} labels only \code{label_genes}.}

\item{label_size, point_size, point_alpha}{Volcano text and point aesthetics
(defaults \code{2.6}, \code{0.9}, \code{0.6}). Shrink \code{label_size} for a dense top-N.}

\item{theme}{Output of \code{ev_theme()}.}
}
\value{
Object of class \code{c("enrichVolcano", "patchwork")} with
\code{attr(., "ev_data")} and \code{attr(., "ev_call")}.
}
\description{
Hero function for \code{enrichVolcano}. Runs the full pipeline:
validate input -> compute pi-score -> adjust p-values -> run enrichment ->
deduplicate -> build volcano + ring panels -> compose patchwork.
}
