% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_table.R
\name{write_table}
\alias{write_table}
\title{Write enrichment results to a CSV file}
\usage{
write_table(x, file)
}
\arguments{
\item{x}{An enrichment object, or the list of them that \code{\link[=run_enrichment]{run_enrichment()}}
returns, which is written as one long table.}

\item{file}{Path of the CSV to write.}
}
\value{
\code{file}, invisibly.
}
\description{
Writes one row per term per contrast, with an \code{enrichment_test} column and
leading edges joined by \verb{;}. \code{\link[=as_enrichment]{as_enrichment()}} reads the file back with
\code{enrichment_test = "custom"} and \code{leading_edge = "leading_edge"}.
}
\examples{
ex <- as_enrichment(read.csv(system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)))
write_table(ex, tempfile(fileext = ".csv"))
}
