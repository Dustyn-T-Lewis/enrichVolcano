% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ring.R
\name{ring_plot}
\alias{ring_plot}
\title{Build an enrichment ring ggplot for one contrast}
\usage{
ring_plot(
  enrich_result,
  contrast,
  max_terms = 10,
  order_by = c("padj", "size", "NES", "alphabetic"),
  order_dir = c("asc", "desc"),
  magnitude = c("neg_log_padj", "nes", "gene_count"),
  color = c("nes", "neg_log_padj", "direction"),
  split_by_direction = TRUE,
  label_clean_fn = NULL,
  theme = ev_theme()
)
}
\arguments{
\item{enrich_result}{Tibble from \code{ev_collapse()} or \code{ev_enrich()}.}

\item{contrast}{Single contrast name.}

\item{max_terms}{Cap on terms shown (default 10; warn at 12, abort at 25).}

\item{order_by}{\code{"padj"} (default), \code{"size"}, \code{"NES"}, or \code{"alphabetic"}.}

\item{order_dir}{\code{"asc"} or \code{"desc"}.}

\item{magnitude}{What arc-thickness encodes: \code{"neg_log_padj"} (default),
\code{"nes"}, or \code{"gene_count"}.}

\item{color}{Arc color encoding: \code{"nes"} (default), \code{"neg_log_padj"},
or \code{"direction"}.}

\item{split_by_direction}{Split up- and down-NES into two arc groups.}

\item{label_clean_fn}{Optional user fn applied to pathway names.}

\item{theme}{Output of \code{ev_theme()}.}
}
\value{
ggplot object.
}
\description{
Uses \code{ggforce::geom_arc_bar} to display the top-N pathways as arc
segments. Magnitude maps to arc thickness, color to NES (or other),
terms split by up/down NES direction.
}
