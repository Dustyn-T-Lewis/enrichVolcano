% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dedup.R
\name{dedup_terms}
\alias{dedup_terms}
\title{Flag redundant terms for display}
\usage{
dedup_terms(
  enrichment,
  gene_sets,
  method = c("enrichmentmap", "collapse_pathways"),
  similarity = c("combined", "jaccard"),
  cutoff = NULL,
  term_threshold = 0.05,
  stats = NULL
)
}
\arguments{
\item{enrichment}{An enrichment object from \code{\link[=as_enrichment]{as_enrichment()}} or \code{\link[=run_enrichment]{run_enrichment()}}.}

\item{gene_sets}{A named list of character vectors, one per term, holding
the full gene sets that were tested. Terms without a set are kept.}

\item{method}{\code{"enrichmentmap"} (gene-set overlap) or \code{"collapse_pathways"}
(conditional enrichment).}

\item{similarity}{For \code{"enrichmentmap"}: \code{"combined"} or \code{"jaccard"}.}

\item{cutoff}{For \code{"enrichmentmap"}: similarity at or above which a term is
redundant. \code{NULL} takes 0.375 for \code{"combined"} and 0.5 for \code{"jaccard"}.}

\item{term_threshold}{Only terms with \code{padj} below this are compared; the rest
are left unflagged (\code{NA}). For \code{"collapse_pathways"} it is also the
conditional p-value threshold.}

\item{stats}{For \code{"collapse_pathways"}: a named list with one ranking (named
numeric vector of gene statistics) per contrast.}
}
\value{
\code{enrichment}, with \code{dedup_status} (\code{"kept"}, \code{"redundant"} or \code{NA}),
\code{merged_into} and \code{similarity} columns in its results, and the settings
stored in \code{metadata$dedup}.
}
\description{
Related gene sets often reach significance together: in GO:BP or
Reactome, one biological signal can light up a dozen overlapping terms.
\code{dedup_terms()} picks one representative per cluster so a figure shows the
signal once. It changes what is drawn, not what was tested: no row is
dropped and no p-value is touched, because the multiple-testing correction
already covered every term.
}
\section{How terms are compared}{

Within each contrast and database, significant terms are walked from the
smallest \code{padj} up. A term is \code{"redundant"} when its gene set is at least
\code{cutoff} similar to a representative already kept, and is recorded as
merged into the most similar one; otherwise it becomes a representative.
Similarity uses the full gene sets, not leading edges.
\itemize{
\item \code{"combined"} (default, cutoff 0.375) is the EnrichmentMap coefficient:
the mean of the Jaccard index and the overlap coefficient
(Merico et al. 2010; Reimand et al. 2019). The overlap coefficient
catches a small set nested inside a large one, which Jaccard misses.
\item \code{"jaccard"} (cutoff 0.5) uses shared genes over all genes in either set.
}
}

\section{\code{method = "collapse_pathways"}}{

Runs \code{\link[fgsea:collapsePathways]{fgsea::collapsePathways()}} within each contrast and database: a term
is redundant when it is no longer enriched once the genes of a more
significant term are conditioned on. This is a statistical criterion, not
an overlap rule, so it needs the ranking each contrast was tested on and
applies only to ranked GSEA results (fgsea, clusterProfiler). It permutes,
so call \code{\link[=set.seed]{set.seed()}} first for reproducible flags. The p-values it computes
decide redundancy only; the reported \code{padj} stay those of the original run.
}

\references{
Merico D, Isserlin R, Stueker O, Emili A, Bader GD (2010). Enrichment Map: a
network-based method for gene-set enrichment visualization and
interpretation. PLoS ONE 5(11):e13984.

Reimand J, Isserlin R, Voisin V, et al. (2019). Pathway enrichment analysis
and visualization of omics data using g:Profiler, GSEA, Cytoscape and
EnrichmentMap. Nature Protocols 14:482-517.
}
