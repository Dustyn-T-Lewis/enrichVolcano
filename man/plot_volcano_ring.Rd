% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano_ring.R
\name{plot_volcano_ring}
\alias{plot_volcano_ring}
\title{Volcano-in-ring composite for one contrast}
\usage{
plot_volcano_ring(
  da,
  enrichment,
  contrast = NULL,
  databases = NULL,
  collapse = TRUE,
  term_threshold = 0.05,
  n_terms = 12,
  terms = NULL,
  labels = "short",
  p_threshold = 0.05,
  logfc_threshold = 0,
  title = NULL,
  subtitle = NULL,
  tag = NULL,
  volcano_radius = 4,
  x_scale = 1,
  y_scale = 1,
  ring_radius = 4.8,
  ring_thickness = 0.55,
  tick_width = 0.3,
  label_headroom = 0.5,
  disc_colour = NULL,
  score_limits = NULL,
  magnitude = NULL,
  arc_order = c("padj", "score"),
  arc_height_range = c(0.4, 1.6),
  show_counts = TRUE,
  point_size = 1.1,
  point_alpha = 0.85,
  label_size = 2.8,
  label_gap = 0.6,
  count_size = 2.4,
  count_x_mult = 0.7,
  count_y_mult = 0.7,
  axis_size = 2.2,
  label_mode = c("none", "top_per_direction", "by_significance", "by_genes"),
  label_n = 5,
  label_rank_by = c("significance", "logfc"),
  label_genes = NULL,
  theme = plot_theme()
)
}
\arguments{
\item{da}{DA results from \code{\link[=as_da]{as_da()}}; the rows for \code{contrast} are drawn.
Points are called significant on \code{padj}, or on \code{p} when \code{padj} is empty.
To colour by another statistic, such as a pi-value, pass it to \code{\link[=as_da]{as_da()}}
as \code{padj}.}

\item{enrichment}{An enrichment object from \code{\link[=as_enrichment]{as_enrichment()}} or \code{\link[=run_enrichment]{run_enrichment()}}.}

\item{contrast}{Which contrast of \code{enrichment} to draw. Needed only when it
holds more than one.}

\item{databases}{Collections to draw from, matched against the \code{database}
column, e.g. \code{c("Hallmark", "GO Slim")}. \code{NULL} (default) draws from all.
Skipped, with a note, when the object has no database labels.}

\item{collapse}{Hide terms flagged \code{"redundant"} by \code{\link[=dedup_terms]{dedup_terms()}}.}

\item{term_threshold}{Terms need \code{padj} below this to be drawn.}

\item{n_terms}{Most unique terms drawn, counted across both directions.}

\item{terms}{Optional character vector of exact term names to draw instead.}

\item{labels}{Term labels. \code{"short"} (default) or \code{"clean"} picks the
style of \code{\link[=clean_label]{clean_label()}}. Your own names replace it for the terms they
name, as a character vector named by term, such as
\code{c(HALLMARK_OXIDATIVE_PHOSPHORYLATION = "Mito respiration")}, or as a data
frame with \code{term} and \code{label} columns. A function takes the term names and
returns one label each. A label with a line break (\verb{\\n}, typed or
written as the two characters in a CSV) is not wrapped again.}

\item{p_threshold}{Significance cutoff for volcano points.}

\item{logfc_threshold}{Effect-size cutoff; a point is called up/down only
when \code{abs(logFC) >= logfc_threshold} as well as significant.}

\item{title, subtitle, tag}{Title, subtitle and panel tag.}

\item{volcano_radius}{Radius of the volcano in plot units.}

\item{x_scale}{Horizontal compression of the point cloud (default 1). Values
below 1 pull points toward the fold-change axis so the widest points clear
the enrichment ring; the up/down axis annotations are unaffected.}

\item{y_scale}{Vertical compression of the point cloud (default 1), anchored
at the fold-change axis. Values below 1 lower the tallest points so they
clear the \verb{-log10 p} label at the top of the volcano.}

\item{ring_radius}{Inner radius of the enrichment ring (default 4.8), where
the leading-edge tick band begins. Raise it to widen the central breathing
gap around the volcano, lower it to close it. Keep it above
\code{volcano_radius * 0.92} so the point cloud clears the ring.}

\item{ring_thickness}{Radial width of the tick band between \code{ring_radius} and
the foot of the coloured arcs (default 0.55). This is the length of the
leading-edge ticks; widen it to make ticks easier to read.}

\item{tick_width}{Line width of the leading-edge ticks (default 0.3).}

\item{label_headroom}{Extra radial room (data units, default 0.5) reserved
beyond the outermost pathway label so its box stays enclosed within the
square panel rather than clipping or spilling into a neighbour. Raise it
when wide label boxes are clipped; lower it to pack the ring tighter.}

\item{disc_colour}{Optional fill for a tinted central disc.}

\item{score_limits}{Length-2 numeric or \code{NULL}; limits of the arc fill scale.
\code{NULL} uses \code{c(-3, 3)} for NES, and otherwise spans the largest absolute
significant score in the whole object, so every contrast shares a scale.}

\item{magnitude}{What arc height encodes: \code{"neg_log_padj"} or \code{"size"}.
\code{NULL} picks \code{"neg_log_padj"} for NES and \code{"size"} otherwise, so fill and
height never repeat the same number for fry or camera results; without
set sizes it falls back to \code{"neg_log_padj"}.}

\item{arc_order}{Angular order of arcs within each up/down half:
\code{"padj"} (default, lowest FDR first) or \code{"score"} (strongest absolute score
first). The up/down split itself is always by direction.}

\item{arc_height_range}{Length-2 numeric \code{c(min, max)} for the shortest
and tallest arc; widen it to exaggerate the magnitude encoding.}

\item{show_counts}{Draw the up/down significant-point count badges.}

\item{point_size, point_alpha}{Volcano point size (default 1.1) and opacity.}

\item{label_size}{Text size of the term labels and the point labels.}

\item{label_gap}{Radial gap between each arc's outer top and its own label
(default 0.6). Anchoring per-arc keeps every leader line the same short
length regardless of arc height; the label box grows outward from this
point so it never overlaps the arc. Widen it to lengthen all leaders.}

\item{count_size}{Text size of the up/down count badges (default 2.4).}

\item{count_x_mult, count_y_mult}{Badge position as a fraction of the volcano
radius (default 0.7).}

\item{axis_size}{Text size of the \code{up}/\code{down}/\verb{log2 FC}/\verb{-log10 p} axis
annotations (default 2.2).}

\item{label_mode}{Which points get a gene label: \code{"none"},
\code{"top_per_direction"} (\code{label_n} up and \code{label_n} down),
\code{"by_significance"} (\code{label_n} in total) or \code{"by_genes"} (the proteins in
\code{label_genes}).}

\item{label_n}{How many points the two top modes label.}

\item{label_rank_by}{Rank points for the top modes by \code{"significance"}
(smallest \code{p}) or \code{"logfc"} (largest absolute fold change).}

\item{label_genes}{Gene symbols or accessions to label with \code{"by_genes"}.}

\item{theme}{Output of \code{\link[=plot_theme]{plot_theme()}}.}
}
\value{
A ggplot.
}
\description{
Draws a differential-abundance volcano embedded in a ring of enrichment
terms. Score-coloured arcs sit around the volcano; tick lines drop from the
arcs to each term's leading-edge genes inside the volcano.
}
\section{Which terms ring the volcano}{

From the chosen contrast, terms in \code{databases} are kept, redundant ones are
hidden when \code{collapse = TRUE} (see \code{\link[=dedup_terms]{dedup_terms()}}), and the \code{n_terms} terms with
the smallest \code{padj} below \code{term_threshold} are drawn, whatever their
direction, so the up and down halves need not match. A term found in two
collections is drawn once. \code{terms} overrides all of this with a hand-picked
set.
}

\examples{
da <- read_example("yvo")$da
ex <- as_enrichment(read.csv(system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)))

plot_volcano_ring(da, ex, contrast = "Training_Young", title = "Training_Young")
}
