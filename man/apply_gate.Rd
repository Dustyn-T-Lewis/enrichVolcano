% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate.R
\name{apply_gate}
\alias{apply_gate}
\title{Flag significance and direction}
\usage{
apply_gate(
  data,
  value = c("adj.P.Val", "P.Value", "pi_eq2"),
  p_cut = 0.05,
  fc_cut = log2(1.5)
)
}
\arguments{
\item{data}{Tidy long tibble with at minimum \code{logFC} and the chosen \code{value}
column.}

\item{value}{One of \code{"adj.P.Val"}, \code{"P.Value"}, \code{"pi_eq2"}.}

\item{p_cut}{Significance threshold (default \code{0.05}).}

\item{fc_cut}{\verb{|logFC|} gate, ignored when \code{value = "pi_eq2"}. Default
\code{log2(1.5)} (~0.585).}
}
\value{
Input tibble with \code{sig} (logical) and \code{direction} (character) columns
appended or overwritten.
}
\description{
Adds two columns to \code{data}: \code{sig} (logical) and \code{direction} (\code{"up"},
\code{"down"}, or \code{"ns"}). The gate logic depends on which significance value
you choose, and the choice should match how that value is defined:
\itemize{
\item \code{"adj.P.Val"} (default) or \code{"P.Value"}: threshold on the p-value
AND a separate \verb{|logFC|} gate. This is the reviewer-safe convention
in differential-abundance proteomics.
\item \code{"pi_eq2"}: threshold on pi-Eq.2 only. Pi-Eq.2 already folds in
fold change (\eqn{\Pi = P^{|\log_2 \mathrm{FC}|}}), so a second
\verb{|logFC|} gate would double-count and exceed Xiao 2014's original
proposal — the convention is to use Pi-Eq.2 directly as an
adjusted-p-like value, NOT to BH-correct it (more stringent than
nominal p, less conservative than BH).
}
Pi-Eq.1 is not accepted as a significance gate — it is unbounded with no
natural threshold; use it for ranking instead (\code{ev_enrich(rank_by = "signed_pi")}
or sort on \code{pi_eq1}).
}
\references{
Xiao Y et al. (2014) A novel significance score for gene identification from
RNA-seq data. \emph{Bioinformatics} 30(6):801-807.
}
