% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_bias_ring.R
\name{plot_bias_ring}
\alias{plot_bias_ring}
\title{Every significant term of one contrast around its volcano}
\usage{
plot_bias_ring(
  da,
  enrichment,
  contrast = NULL,
  databases = NULL,
  collapse = TRUE,
  term_threshold = 0.05,
  title = NULL,
  subtitle = NULL,
  theme = plot_theme(),
  ...
)
}
\arguments{
\item{da}{DA results from \code{\link[=as_da]{as_da()}}; the rows for \code{contrast} are drawn.
Points are called significant on \code{padj}, or on \code{p} when \code{padj} is empty.
To colour by another statistic, such as a pi-value, pass it to \code{\link[=as_da]{as_da()}}
as \code{padj}.}

\item{enrichment}{An enrichment object from \code{\link[=as_enrichment]{as_enrichment()}} or \code{\link[=run_enrichment]{run_enrichment()}}.}

\item{contrast}{Which contrast of \code{enrichment} to draw. Needed only when it
holds more than one.}

\item{databases}{Collections to draw from, matched against the \code{database}
column, e.g. \code{c("Hallmark", "GO Slim")}. \code{NULL} (default) draws from all.
Skipped, with a note, when the object has no database labels.}

\item{collapse}{Hide terms flagged \code{"redundant"} by \code{\link[=dedup_terms]{dedup_terms()}}.}

\item{term_threshold}{Terms need \code{padj} below this to be drawn.}

\item{title}{Title.}

\item{subtitle}{Subtitle. \code{NULL} reports the up and down term counts.}

\item{theme}{Output of \code{\link[=plot_theme]{plot_theme()}}.}

\item{...}{Other \code{\link[=plot_volcano_ring]{plot_volcano_ring()}} arguments that set the layout, such
as \code{ring_radius}, \code{arc_height_range}, \code{p_threshold} or \code{point_size}.}
}
\value{
A ggplot.
}
\description{
Draws all significant terms of a contrast as a ring, without names, to show
whether the contrast leans up or down. Every arc gets the same angle, so the
up and down halves grow with their term counts. Fill and arc height are the
score over the strongest drawn score: the strongest term is full colour and
full height, and every other term is a fraction of it.
}
\examples{
da <- read_example("yvo")$da
ex <- as_enrichment(read.csv(system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)))
plot_bias_ring(da, ex, contrast = "Aging", title = "Aging")
}
