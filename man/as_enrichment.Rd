% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_enrichment.R
\name{as_enrichment}
\alias{as_enrichment}
\title{Convert enrichment results into an \code{enrichment} object}
\usage{
as_enrichment(
  x,
  enrichment_test = NULL,
  database = NULL,
  term = "term",
  score = "score",
  padj = "padj",
  p = "p",
  size = "size",
  direction = "direction",
  leading_edge = NULL,
  score_type = NULL
)
}
\arguments{
\item{x}{A long table with a \code{contrast} column, or a named list of tables,
one per contrast, whose names become the contrasts.}

\item{enrichment_test}{The method that produced \code{x}: one of \code{"fgsea"},
\code{"fry"}, \code{"mroast"}, \code{"camera"}, \code{"cameraPR"}, \code{"gseaResult"}, \code{"ora"},
\code{"custom"}. \code{NULL} recognises it from the columns.}

\item{database}{A label for every row, such as \code{"Hallmark"}, when the input
has no \code{database} column.}

\item{term, score, padj, p, size, direction, leading_edge}{Column names, used only
when \code{enrichment_test} is \code{"custom"} or \code{"ora"}. \code{p}, \code{size} and
\code{leading_edge} are optional; a leading-edge column may be a list or a
string separated by \verb{;}, \code{/} or \code{|}. Pass \code{direction = NULL} to take
direction from the score sign.}

\item{score_type}{Axis and legend label for a custom score, e.g. \code{"NES"}.}
}
\value{
An S7 \code{enrichment} object. Every construction and every edit is
validated. \verb{@results} has one row per term per contrast:
\itemize{
\item \code{contrast}, \code{database}, \code{term}: character. \code{database} may be \code{NA}.
\item \code{score}: numeric. NES for fgsea and clusterProfiler; signed
\eqn{-\log_{10}}(FDR) for limma rotation/competitive tests and ORA.
\item \code{p}, \code{padj}: numeric in [0, 1]; \code{NA} allowed.
\item \code{size}: numeric set size.
\item \code{direction}: \code{"up"} or \code{"down"}, agreeing with the sign of \code{score}.
\item \code{leading_edge}: list of character vectors, empty when the test has none.
}

Extra columns, such as \code{dedup_status}, are kept. \verb{@metadata} holds
\code{enrichment_test}, \code{score_type} (the axis and legend label for \code{score})
and \code{dedup} (the settings \code{\link[=dedup_terms]{dedup_terms()}} used, or \code{list(method = "precomputed")} when the input already carried \code{dedup_status}).
}
\description{
The one entry point for results from any enrichment tool. Hand it one
table per contrast and it returns a validated \code{enrichment} object that
every plot in the package reads.
}
\section{Which test produced the table}{

The test is recognised from the columns it returns:
\itemize{
\item \strong{fgsea}: \verb{pathway, pval, padj, NES, size, leadingEdge}. Score is NES.
\item \strong{fry} (\code{PValue.Mixed}), \strong{mroast} (\code{PropUp}), \strong{camera} estimating
its own inter-gene correlation (\code{Correlation}): \verb{NGenes, Direction, PValue, FDR}, set names in the row names (or a \code{term} column, which a
long table with a \code{contrast} column must have). None of these report an effect
size or a leading edge, so the score is signed \eqn{-\log_{10}}(FDR) and
leading edges are empty.
\item \strong{camera} with its default fixed correlation and \strong{cameraPR} return
identical columns. Say which with \code{enrichment_test = "camera"} or
\code{"cameraPR"}.
\item \strong{gseaResult} (clusterProfiler \code{GSEA()}, \code{gseGO()}, ...): read from the
object's \code{result} table, or from that table exported as a data frame.
Score is NES; leading edges come from \code{core_enrichment}.
\item \strong{ora} (enrichR, \code{enrichGO()}, g:Profiler, ...): over-representation has
no direction of its own, so supply a \code{direction} column (run up- and
down-regulated genes separately and stack them). Score is signed
\eqn{-\log_{10}}(padj).
\item \strong{custom}: any table, mapped with the column arguments. Direction comes
from the \code{direction} column when present, otherwise from the score sign.
}

fry and mroast are self-contained tests (is this set changed at all?);
camera, cameraPR and fgsea are competitive (is it changed more than the
genes outside it?). The test is recorded so figures can say which.
}

\examples{
path <- system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)
ex <- as_enrichment(read.csv(path))
ex
}
\references{
Wu T, Hu E, Xu S, et al. (2021). clusterProfiler 4.0: A universal
enrichment tool for interpreting omics data. The Innovation 2(3):100141.
\doi{10.1016/j.xinn.2021.100141}
}
