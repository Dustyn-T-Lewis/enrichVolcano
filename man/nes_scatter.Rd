% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nes_scatter.R
\name{nes_scatter}
\alias{nes_scatter}
\title{Compare two contrasts term by term}
\usage{
nes_scatter(
  enrichment,
  x,
  y,
  comparison = c("concordance", "reversal"),
  databases = NULL,
  collapse = TRUE,
  p_threshold = 0.05,
  color_by = "significance",
  shape_by = "database",
  label_min_size = 15,
  max_labels = 20,
  theme = volcano_ring_theme()
)
}
\arguments{
\item{enrichment}{An \link{enrichment} object holding both contrasts.}

\item{x, y}{Contrast names for the horizontal and vertical axes.}

\item{comparison}{\code{"concordance"} or \code{"reversal"}; see Description.}

\item{databases}{Collections to draw from, e.g. \code{c("Hallmark", "GO Slim")};
\code{NULL} (default) draws from all. Skipped, with a note, when the object has
no database labels.}

\item{collapse}{Hide terms that \code{\link[=dedup]{dedup()}} flagged redundant in one contrast
and kept as a representative in neither.}

\item{p_threshold}{Significance cutoff on \code{padj}.}

\item{color_by}{Column that fills significant points: \code{"significance"}
(significant in \code{x} only, \code{y} only, or both), any per-term column such as
\code{"database"}, or \code{NULL} for one colour. Per-term columns are read from
the \code{x} contrast.}

\item{shape_by}{Column mapped to point shape (up to five values), or \code{NULL}.}

\item{label_min_size}{Smallest gene set that gets a label.}

\item{max_labels}{Most labels drawn.}

\item{theme}{Output of \code{\link[=volcano_ring_theme]{volcano_ring_theme()}}; supplies the base font.}
}
\value{
A ggplot.
}
\description{
Plots each term's score in one contrast against its score in another.
Terms in the top-right and bottom-left quadrants move the same way in both
contrasts; terms in the other two move in opposite directions.
}
\details{
\code{comparison} sets the question the figure asks:
\itemize{
\item \code{"concordance"}: do two conditions regulate the same pathways, e.g.
training in young against training in old? Same-direction quadrants are
"concordant", the reference line is \eqn{y = x}, and the subtitle reports
the share of significant terms that are concordant.
\item \code{"reversal"}: does one condition undo another, e.g. aging against
training? Opposite-direction quadrants are "reversed", same-direction
ones "exacerbated", the reference line is \eqn{y = -x}, and the subtitle
reports the share reversed.
}
}
\section{What is drawn}{

\itemize{
\item Points: every term scored in both contrasts. Non-significant terms are
small and grey; significant ones are sized by gene-set size and filled by
\code{color_by}.
\item Shading: same-direction quadrants are tinted, the others left white.
\item Corner labels: each quadrant's name and its count of significant terms.
\item Dashed line: \eqn{y = x}, or \eqn{y = -x} for a reversal.
\item Labels: the most significant terms with at least \code{label_min_size} genes.
\item Subtitle: Spearman's \eqn{\rho} across all plotted terms, with a 95\%
interval when the correlation package is installed, its p-value, the
share of significant terms that are concordant (or reversed), and the
term counts.
}

A term is significant in a contrast when its \code{padj} is below \code{p_threshold}.
If the object has no adjusted p-values at all, nominal p-values are used
and a note says so.
}

\examples{
ex <- as_enrichment(read.csv(system.file("extdata", "examples", "yvo_fgsea.csv.gz",
  package = "enrichVolcano"
)))
nes_scatter(ex, "Training_Young", "Training_Old")
nes_scatter(ex, "Aging", "Training_Old", comparison = "reversal")
}
