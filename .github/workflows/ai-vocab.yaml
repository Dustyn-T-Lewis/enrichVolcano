name: ai-vocab-check
on: [push, pull_request]
jobs:
  grep:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Forbid AI-vocabulary
        run: |
          if grep -rE 'leverage|robust|comprehensive|seamless|streamline|delve|utilize|harness' R/ man/ vignettes/ README.md ; then
            echo "::error::AI-vocabulary patterns detected. Re-word the flagged passages."
            exit 1
          fi
